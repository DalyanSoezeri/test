function save(){
    var all="";
   const index = document.getElementById('countRows').value;

    for (let rows = 0; rows <= rows; rows++) {
        for (let colums = 1; colums <= 5; colums++) {
            console.log( );
            all+=document.getElementById(`${rows}_${colums}`).value;
        }
    }
    
    alert(hallo);
    
}