function save(){
    var all="";
    let index = 0;

    for (let rows = 0; rows <= index; rows++) {
            for (let colums = 1; colums <= 5; colums++) {
            console.log(`${rows} and ${colums}`);
        }
    }
    
}

save()